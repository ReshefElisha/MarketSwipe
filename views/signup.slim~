h3 Don't have an account yet? Sign up!
form.form-horizontal(action=url('/signup') method='post')
  legend Signup
  .form-group
    label.control-label(for='pitt_id') Pitt ID
    input.form-control#pitt_id(name='pitt_id' type='text' placeholder='abc123' required=true)
  .form-group
    label.control-label(for='name') Full Name
    input.form-control#name(name='name' type='text' placeholder='abc123' required=true)
  .form-group
    label.control-label(for='password') Password:
    input.form-control#password(name='password' type='password' required=true)
  .form-group
    label.control-label(for='password') Repeat Password:
    input.form-control#rpassword(name='rpassword' type='password' required=true)
  .form-group
    button.btn.btn-primary(type='submit') Sign Up for MarketSwipeMe!
